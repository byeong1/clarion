{
    "mcpServers": {
        "clarion": {
            "command": "npx",
            "args": ["--yes", "tsx", "${CLAUDE_PLUGIN_ROOT}/src/main.ts"],
            "cwd": "${CLAUDE_PLUGIN_ROOT}"
        }
    }
}
