{
    "mcpServers": {
        "clarion": {
            "command": "npx",
            "args": ["tsx", "${CLAUDE_PLUGIN_ROOT}/src/main.ts"],
            "env": {
                "NODE_PATH": "${CLAUDE_PLUGIN_DATA}/node_modules"
            }
        }
    }
}
